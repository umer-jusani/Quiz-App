import React, { useState } from "react";

const Quiz = ({ data }) => {
  const [currentQuestion, setcurrentQuestions] = useState(0);

  return (
    <div className="quiz-container">
      <h1>{currdata.currdata}</h1>
    </div>
  );
};

export default Quiz;
